# 电子书封面校验修复实施计划

1. 在后端 Maven 依赖中加入 WebP ImageIO 解码器。
   - 验证：Maven 可解析依赖并编译。
2. 将 `CoverStorage` 改为使用统一 ImageIO 解码校验 JPEG、PNG、WebP，删除 RIFF 手写解析。
   - 验证：真实三种图片通过，伪造签名和伪造 WebP 被拒绝。
3. 将封面清理回调注册提前到数据库更新前。
   - 验证：模拟更新异常并触发回滚时，新封面被调用删除；提交时旧封面被调用删除。
4. 更新集成测试与服务单元测试，并运行 Maven 全量测试、前端构建和上传接口冒烟。
